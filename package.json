{
  "name": "olympusbonds",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ olympusbonds",
    "create-local": "graph create --node http://localhost:8020/ olympusbonds",
    "remove-local": "graph remove --node http://localhost:8020/ olympusbonds",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 olympusbonds"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
